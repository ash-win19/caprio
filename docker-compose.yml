services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: caprio
      POSTGRES_PASSWORD: caprio
      POSTGRES_DB: caprio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
